Version: 5.1.0
Changes:
- Incremented semantic version number to 5.1.0
- Scaled up grid card font sizes (Titles to text-sm, authors to text-xs, stats to text-[11px])
- Micro-adjusted card height to 155px to ensure no text overflows or clipping occurs

Version: 5.2.0
Changes:
- Incremented semantic version number to 5.2.0
- Made all Detailed List View columns (except Action) fully interactive and sortable
- Added active visual sort chevron indicators next to column headers
- Synchronized direct column header clicks with the main sorting dropdown selection
- Expanded sorting algorithm to support bidirectional field sorting (Title, Author, Narrator, Rating, Length)

Version: 5.3.0
Changes:
- Incremented semantic version number to 5.3.0
- Added check to support playing and downloading local audiobooks from "bookFiles" folder configured in library.json
- Integrated a gorgeous floating custom media player with interactive progress tracking, play/pause and 30s seek controls
- Restructured grid cards and detail modal to split and render all categories into individual compact pills
- Changed seriesBadgeClass text size to 13px in createCompactGridCard
- Updated catBadgeClass and seriesBadgeClass styles to use high-contrast glass gradients when hasImage is false
- Updated series template rendering to separate Series Name and Volume into separate distinct badges with spacing

Version: 5.4.0
Changes:
- Incremented semantic version number to 5.4.0
- Standardized category pills style: now always renders using the glassmorphic
  theme (backdrop-blur, translucent white background, and borders) even when
  a local book cover (backgroundImage) is present.

Version: 5.5.0
Changes:
- Incremented semantic version number to 5.5.0
- Increased card top cover height by 50% (from 80px to 120px) and adjusted total grid card height to 195px.
- Standardized category pills wrapping behavior across cards with background images and standard gradients.
- Removed categories overlay badge from the dynamic cover visualizer inside the modal.

Version: 5.6.0
Changes:
- Incremented semantic version number to 5.6.0
- Changed application name and references from "Audible" to "eBook" across title, headers, metadata, and controls
- Updated HTML page title to "eBook Library Dashboard"
- Updated the branding icon from headphones to a book logo
- Renamed stats panels from "Total Audiobooks" to "Total eBooks" for platform consistency

Version: 5.7.0
Changes:
- Incremented semantic version number to 5.7.0
- Added a custom-designed vector favicon in SVG format inside a high-compatibility URI that matches the "book-open" dashboard brand mark









